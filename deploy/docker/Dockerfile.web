# Image Nginx : nginx.conf complet (pas conf.d seul) pour eviter les effets de bord de l'entrypoint.
FROM nginx:alpine
RUN rm -f /etc/nginx/conf.d/default.conf
COPY deploy/docker/nginx.conf /etc/nginx/nginx.conf
